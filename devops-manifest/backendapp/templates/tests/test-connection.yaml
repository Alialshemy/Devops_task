apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backendapp.fullname" . }}-test-connection"
  labels:
    {{- include "backendapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backendapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
